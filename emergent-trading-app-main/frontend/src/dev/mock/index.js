/**
 * Demo Mode mock data — synthetic generators for UI development only.
 * Import ONLY when Demo Mode is enabled (see DemoModeContext).
 */
export * from "./quantMock";
export * from "./geometryMock";
